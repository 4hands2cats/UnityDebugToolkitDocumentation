The nav bar is a componenet that works really well with the bindable buttons. 

The component binds **NavBarButtons** with panels. 

It's fully compatible with the Unity navigation.

This simple component is the heart of any big and organized UI.